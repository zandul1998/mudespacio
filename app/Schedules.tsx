'use client';
import {useEffect,useState} from 'react';
import {dayLabels,scheduleStatusLabels,type Schedule,wa} from './shared';
export default function Schedules(){const[data,setData]=useState<Schedule[]|null>(null),[error,setError]=useState(false);async function load(){setError(false);try{const r=await fetch('/api/schedules');if(!r.ok)throw Error();setData((await r.json() as {schedules:Schedule[]}).schedules)}catch{setError(true)}}useEffect(()=>{void load()},[]);
if(error)return <div className="schedule-empty" role="status"><p>No pudimos cargar los horarios.</p><button className="home-text-link" onClick={load}>Volver a intentar</button></div>;
if(data===null)return <div className="schedule-loading" role="status" aria-label="Cargando horarios">{[0,1,2].map(i=><span key={i}/>)}</div>;
if(!data.length)return <div className="schedule-empty"><h3>Estamos organizando los próximos grupos.</h3><p>Escribinos y te avisamos apenas publiquemos nuevos horarios.</p><a className="home-button light" href={wa('¡Hola Mud! Quisiera que me avisen cuando publiquen nuevos horarios de clases.')} target="_blank" rel="noreferrer">Quiero recibir novedades</a></div>;
const groups=Object.entries(Object.groupBy(data,s=>String(s.day))).sort(([a],[b])=>Number(a)-Number(b));return <div className="schedule-grid">{groups.map(([day,slots])=><article className="schedule-day" key={day}><h3>{dayLabels[Number(day)]}</h3><div>{slots!.map(slot=>{const available=slot.status!=='full';const message=slot.status==='forming'?`¡Hola Mud! Me interesa el grupo en formación de ${dayLabels[slot.day]} de ${slot.start} a ${slot.end}.`:`¡Hola Mud! Quisiera consultar por el horario de ${dayLabels[slot.day]} de ${slot.start} a ${slot.end}.`;return <div className={'schedule-slot status-'+slot.status} key={slot.id}><div><strong>{slot.start} a {slot.end}</strong>{slot.note&&<small>{slot.note}</small>}</div><span>{scheduleStatusLabels[slot.status]}</span>{available&&<a href={wa(message)} target="_blank" rel="noreferrer">{slot.status==='forming'?'Me interesa':'Consultar'}</a>}</div>})}</div></article>)}</div>}
