import {bindings,guard,listWaitlistEntries} from '../../../server';
import {validateWaitlistEntry} from '../../../validation';

export async function GET(request:Request){const denied=await guard(request);if(denied)return denied;try{return Response.json({waitlist:await listWaitlistEntries()},{headers:{'Cache-Control':'no-store'}})}catch{return Response.json({error:'No pudimos cargar la lista.'},{status:503})}}
export async function POST(request:Request){const denied=await guard(request);if(denied)return denied;try{const input=validateWaitlistEntry(await request.json()),db=bindings().DB,id=input.id||crypto.randomUUID(),now=new Date().toISOString();const existing=input.id?await db.prepare('SELECT id FROM waitlist_entries WHERE id=?').bind(id).first():null;if(input.id&&!existing)return Response.json({error:'Ese registro ya no existe.'},{status:404});await db.prepare('INSERT INTO waitlist_entries (id,student_id,schedule_id,status,source,notes,created_at,updated_at) VALUES (?,?,?,?,?,?,?,?) ON CONFLICT(id) DO UPDATE SET student_id=excluded.student_id,schedule_id=excluded.schedule_id,status=excluded.status,source=excluded.source,notes=excluded.notes,updated_at=excluded.updated_at').bind(id,input.student_id,input.schedule_id,input.status,input.source,input.notes,now,now).run();return Response.json({id})}catch(e){return Response.json({error:e instanceof TypeError?e.message:'No pudimos guardar el registro.'},{status:e instanceof TypeError?400:500})}}
export async function DELETE(request:Request){const denied=await guard(request);if(denied)return denied;try{const id=new URL(request.url).searchParams.get('id')||'';if(!/^[0-9a-f-]{36}$/.test(id))return Response.json({error:'Registro inválido.'},{status:400});const result=await bindings().DB.prepare('DELETE FROM waitlist_entries WHERE id=?').bind(id).run();if(!result.meta.changes)return Response.json({error:'Ese registro ya no existe.'},{status:404});return Response.json({ok:true})}catch{return Response.json({error:'No pudimos eliminar el registro.'},{status:500})}}
