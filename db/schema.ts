import {sqliteTable,text,integer,index,uniqueIndex} from 'drizzle-orm/sqlite-core';
export const products=sqliteTable('products',{id:text('id').primaryKey(),name:text('name').notNull(),description:text('description').notNull().default(''),price:integer('price'),variants:text('variants').notNull().default(''),status:text('status').notNull().default('order'),published:integer('published').notNull().default(0),image:text('image').notNull().default(''),sort:integer('sort').notNull().default(0)},t=>[index('products_visibility_order').on(t.published,t.sort)]);
export const schedules=sqliteTable('schedules',{id:text('id').primaryKey(),day:integer('day').notNull(),start:text('start').notNull(),end:text('end').notNull(),status:text('status').notNull().default('forming'),note:text('note').notNull().default(''),published:integer('published').notNull().default(0),sort:integer('sort').notNull().default(0)},t=>[index('schedules_visibility_order').on(t.published,t.day,t.sort)]);
export const teamMembers=sqliteTable('team_members',{id:text('id').primaryKey(),email:text('email').notNull(),name:text('name').notNull().default(''),role:text('role').notNull().default('team'),active:integer('active').notNull().default(1),createdAt:text('created_at').notNull(),updatedAt:text('updated_at').notNull()},t=>[uniqueIndex('team_members_email').on(t.email),index('team_members_access').on(t.active,t.role)]);
